<Window x:Class="AutomationProfileManager.Views.ApplicationSelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource LOC_APM_SelectApplicationTitle}" Height="550" Width="750"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        MinHeight="400" MinWidth="500"
        Background="#FF1E1E1E">
    
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Styles/DarkTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="PromptTextBlock" Grid.Row="0" 
                   FontSize="14" FontWeight="SemiBold" Margin="0,0,0,10" TextWrapping="Wrap"/>

        <!-- Search Box -->
        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="{DynamicResource LOC_APM_SearchLabel}" Margin="0,0,0,3"/>
            <TextBox x:Name="SearchTextBox" Padding="5" TextChanged="SearchTextBox_TextChanged"/>
        </StackPanel>

        <DataGrid x:Name="ApplicationsDataGrid" Grid.Row="2" 
                  AutoGenerateColumns="False"
                  SelectionMode="Single"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  Margin="0,0,0,15">
            <DataGrid.Columns>
                <DataGridTextColumn Header="{DynamicResource LOC_APM_ColumnName}" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="{DynamicResource LOC_APM_Path}" Binding="{Binding ExecutablePath}" Width="2*"/>
                <DataGridTextColumn Header="{DynamicResource LOC_APM_Process}" Binding="{Binding ProcessName}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Orientation="Horizontal" Grid.Row="3" HorizontalAlignment="Right">
            <Button Content="{DynamicResource LOC_APM_Select}" Click="OK_Click" Width="100" Margin="0,0,10,0" IsDefault="True"/>
            <Button Content="{DynamicResource LOC_APM_Cancel}" Click="Cancel_Click" Width="90" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
